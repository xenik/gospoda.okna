<p id="notice"><%= notice %></p>

<%= form_for @order do |f| %>
 
<div class="field">
  <%= f.label :name %><br />
  <%= f.text_field :name %>
</div>

<% end %>

<%= link_to 'Edit', edit_order_path(@order) %> |
<%= link_to 'Back', orders_path %>
