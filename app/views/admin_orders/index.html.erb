<div style="margin: 0 auto;outline: 1px solid red;width: 600px;padding: 10px;">
<h1>Listing admin_orders</h1>

<table>
  <tr>
    <th>id |</th>
    <th> name |</th>
    <th> Show |</th>
    <th> Edit |</th>
    <th> Destroy</th>
  </tr>

<% @orders.each do |order| %>
  <tr>
    <td><%= order.id %></td>
    <td><%= order.name %></td>
    <td><%= link_to 'S', order %></td>
    <td><%= link_to 'E',  edit_order_path(order) %></td>
    <td><%= link_to 'D', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />
<!-- #edit_order_path(order) -->
<%= link_to 'New order', new_order_path %>
</div>
